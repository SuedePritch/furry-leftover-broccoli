const User = require('./User');
const Order = require('./Order');
const Category = require('./Category');
const Products = require('./Products');
const Store = require('./Store');

module.exports = { User, Order, Category, Products, Store };
